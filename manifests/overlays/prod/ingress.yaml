apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 8221069-ingress
  namespace: "82210669"
spec:
  ingressClassName: nginx
  rules:
  - host: "prod.ce-82210669-sa.kubepia.net"
    http:
      paths:
      - path: /
        backend:
          service:
            name: prod-my-82210669
            port:
              number: 8080
        pathType: Prefix
